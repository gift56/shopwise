"use client"

const Searchbar = () => {
  const handleSubmit = () => {};
  return (
    <form className="flex flex-wrap gap-4 mt-12" onSubmit={handleSubmit}>
      Searchbar
    </form>
  );
};

export default Searchbar;
